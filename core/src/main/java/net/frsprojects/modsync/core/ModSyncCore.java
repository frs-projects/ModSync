package net.frsprojects.modsync.core;

/** Entry point marker for the loader-independent half of ModSync. */
public final class ModSyncCore {
    private ModSyncCore() {}

    /** Manifest format this build reads and writes. */
    public static final int MANIFEST_FORMAT_VERSION = 1;
}
