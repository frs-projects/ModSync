# Loom refuses to set up Minecraft when the daemon JVM is older than the version
# that Minecraft itself requires, and 26.x needs Java 25. Per-node toolchains
# still emit 17/21/25 bytecode via `options.release`.
toolchainVersion=25
